# ============================================================
# ALT PRINT - Python Dependencies (verified conflict-free)
# Last audited: 2026-06-11
#
# AWS storage compatibility chain (verified via PyPI metadata):
#   aioboto3==15.5.0     requires  aiobotocore[boto3]==2.25.1
#   aiobotocore==2.25.1  requires  botocore>=1.40.46,<1.40.62
#   boto3==1.40.61       requires  botocore>=1.40.61,<1.41.0
#   INTERSECTION         =>        botocore==1.40.61  ✓ (single version satisfies all)
#
# DO NOT change any of these four versions independently.
# They must move together as a locked group.
# ============================================================

# ── Core Framework ───────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# ── Database ─────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
psycopg2-binary==2.9.10

# ── Cache & Message Broker ───────────────────────────────────
redis==5.2.0
celery==5.4.0
flower==2.0.1

# ── Authentication & Security ────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==43.0.3
pydantic-settings==2.6.1

# ── Storage — LOCKED GROUP (do not change independently) ─────
aioboto3==15.5.0
aiobotocore==2.25.1
boto3==1.40.61
botocore==1.40.61

# ── HTTP Client ──────────────────────────────────────────────
httpx==0.28.0
aiohttp==3.11.10

# ── Document Processing ──────────────────────────────────────
pypdf==5.1.0
python-docx==1.1.2
Pillow==11.0.0
lxml==5.3.0

# ── Payment ──────────────────────────────────────────────────
razorpay==1.4.2

# ── Data Validation ──────────────────────────────────────────
pydantic[email]==2.10.2
email-validator==2.2.0
phonenumbers==8.13.49

# ── Utilities ────────────────────────────────────────────────
python-slugify==8.0.4
pytz==2024.2

# ── Testing ──────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
factory-boy==3.3.1

# ── Dev Tools ────────────────────────────────────────────────
black==24.10.0
ruff==0.8.0
celery[redis]
flower

bcrypt==4.0.1
